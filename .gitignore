# Dart/Flutter
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies

# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store
Thumbs.db

# 워크스페이스 lockfile 은 커밋 (앱 재현성). 필요시 개별 무시하지 말 것.

# 대용량 원본 백업(누끼 전 2048px) — 커밋 제외
packages/app/assets/images/bugs/_raw_backup/

# Supabase 로컬 자격증명(anon key 등) — 커밋 금지, --dart-define-from-file 로 주입
packages/app/supabase.env.json
