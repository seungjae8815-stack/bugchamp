name: core_models
description: Bug Champ 순수 데이터 모델 (Species, IndividualBug, Material, Trap, Field). Flutter/Hive/Riverpod 의존 금지.
version: 0.1.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.11.5

dependencies:
  meta: ^1.16.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
