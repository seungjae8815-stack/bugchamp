name: core_gathering
description: Bug Champ 채집 방치 루프 (결정론). 경과시간 -> 재료/조우 개체 산출. core_models 에만 의존. Flutter 금지.
version: 0.1.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.11.5

dependencies:
  core_models:
    path: ../core_models
  meta: ^1.16.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
