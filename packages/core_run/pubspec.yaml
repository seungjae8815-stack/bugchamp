name: core_run
description: Bug Champ v2 횡스크롤 런 엔진 (순수 Dart, 결정론). 서식지 HP/데미지/보상/업그레이드 수식. core_models 의존, Flutter 금지.
version: 0.1.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.11.5

dependencies:
  core_models:
    path: ../core_models
  meta: ^1.16.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
