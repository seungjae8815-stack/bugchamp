name: core_battle
description: Bug Champ 결정론적 전투 엔진. simulate(seed, teamA, teamB) -> BattleResult. core_models 에만 의존. Flutter 금지.
version: 0.1.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.11.5

dependencies:
  core_models:
    path: ../core_models

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
