# CLAUDE.md — Bug Champ (곤충 채집 배틀 게임)

> ⚠️ **사용자에게 보이는 모든 글은 한국어로 쓴다** — 최종 답변뿐 아니라 도구 호출 사이의 짧은 진행 안내 한 줄까지.
> 코드 식별자·명령어·경로만 원문. (사장님이 여러 번 지적: 긴 작업 중 안내가 영어로 샌다.)

> 이 파일은 Claude Code가 매 세션 로드하는 프로젝트 헌법이다.
> 여기 적힌 **기획 확정사항·아키텍처 규칙**은 사용자의 명시적 승인 없이 변경하지 않는다.

---

## 1. 프로젝트 개요

- **장르**: 방치형 채집 + 개체 수집·육성 + 비동기 PvP
- **플랫폼**: iOS / Android, 글로벌 출시
- **언어**: ko / en / ja (텍스트 하드코딩 금지, ARB 기반)
- **개발 형태**: 1인 개발, Windows + PowerShell 환경

---

## 2. 게임 핵심 규칙 (기획 확정 — 임의 변경 금지)

수치의 **구체적 값**은 코드가 아니라 `packages/app/assets/data/*.json` 에 존재한다.
아래는 **규칙(모델·공식)의 정의**이며, 계수를 바꿀 땐 JSON을 바꾼다.

### 2.1 개체 시스템 — 곤충 = 종(Species) × 개체 변수(Individual)
- **종(Species)**: 5등급(일반/고급/희귀/영웅/전설), 기본 스탯 `HP/ATK/DEF/SPD`,
  주특기 1개(`치기`/`집기`/`던지기` → 전투에서 **선호 스탠스**로 매핑), 사이즈 범위(mm, min~max).
- **개체(IndividualBug)**:
  - **오행 속성(Element)**: 목/화/토/금/수 중 1개(랜덤). 전투 상극(克)·상생(生)에 사용 → §2.3.
  - **사이즈**: 종 범위 내 **정규분포** 롤 → 스탯 배율 **0.85 ~ 1.20** 로 선형 매핑.
  - **포텐셜**: 1~5성. 강화 상한 레벨 = `포텐셜 × 10`. 돌파(§2.7)로 성충 수련 상한 확장.
  - **기질**: 호전적/신중/교활/우직/변덕 — 전투 오토 **스탠스 선택 성향**.
  - **성별**: ♂/♀ (브리딩 조건).
  - **생애주기**: 알 → 유충 → 번데기 → 성충(부화기·경과시간으로 진화 §2.7).
- **종 고유 패시브(2026-08-15 확정)**: 종마다 능력치 하나를 올린다(`species.json → passive`).
  **장착(펫)했을 때만** 붙고, 같은 종을 여러 마리 장착하면 합산된다.
  - 이유: 20종이 등급·기본스탯·주특기 말고는 구분이 없어서, 목표가 "장수풍뎅이를 갖고 싶다"가
    아니라 "전설을 갖고 싶다"가 됐다. 종 20개가 사실상 **등급 5개**로 압축돼 있었다.
  - **보스 피해는 곱으로 얹는다(2026-09-22 사장님 확정)** — 나머지 확률·계수는 가산. 보스 피해는 강화로
    1.0 → 11.0 까지 자라서 가산이면 물장군 "+25%" 가 극한에서 +2~3% 였다. 스킬 끈기도 같은 함수를 탄다.
  - `stat` 은 `UpgradeKind.key` 문자열(core_models 는 core_run 을 모른다). 오타는 로딩을 통과하고
    **패시브만 조용히 사라지므로**, `data_test` 의 키 유효성 검사가 반드시 있어야 한다.
  - ⚠️ **적응형 몬스터 체력 기준(§7) 밖**에 적용한다 — 기준에 넣으면 어떤 종을 껴도 결과가
    같아져서 종을 고르는 의미가 통째로 사라진다(= 이 시스템을 만든 이유가 무너진다).
- **이색 개체(2026-08-31 확정)**: 아주 낮은 확률(야생 1/300 · 짝짓기 1/60 · 부모가 이색이면
  1/12 · 뽑기 1/30)로 나오는 **색이 다른 개체**. 무지개/알비노 반반. 수치는 `pets.json → variant*`.
  - **능력치가 월등히 세다**(펫 기여 +60%, 전투는 `variantBattleScale` 로 절반만 = +30%).
    외형만 두면 계속 도전할 이유가 약하다는 판단 — 1/300 이라 흔해질 위험이 없다.
  - ⚠️ **위조를 막을 수 없다**(곤충 롤이 기기 권위 §2.5 특성과 같은 구멍). 그래서 전투 반영을
    `variantBattleScale` 로 **따로** 뺐다 — PvP 에서 문제가 생기면 이 값만 0 으로 내려
    전투만 끈다(펫 기여는 남는다). 서버 발급으로 전환할 때 특성과 함께 봉인한다.
  - rng 소비는 기존 롤 **맨 뒤**에 붙인다 — 앞에 끼우면 같은 seed 의 브리딩 슬롯 결과가 달라진다.
- **도감(2026-08-15 확정, 2026-08-31 이색 축 추가)**: 종별 **역대 최대 크기·최고 포텐셜·
  정복(성충까지 키움)·이색 획득** 기록. 세 축(발견·정복·이색)을 다 채우면 **완료(👑)** 다 —
  하나씩 채워 가는 종착점이 눈에 보여야 모을 이유가 생긴다.
  - **정복 기준(2026-08-31 상향)**: 성충까지 키움 → **수련 `conquerLevel`(10) 달성**.
    예전 기준은 부화만 시키면 자동으로 달성돼서 도감 정복이 "알 20개를 부화했다"의 다른 이름이었다.
    수련은 골드가 실제로 드는 행동이라 여기가 걸려야 도감이 목표가 된다.
    ⚠️ **기준을 올리기 전 기록은 그대로 인정한다**(`DexEntry.legacyConquered`) — 안 그러면
    업데이트 순간 기존 유저의 도감 영구 보너스가 사라져 "내 공격력이 줄었다"가 된다.
    스키마를 올리지 않고 파싱에서 유도한다(`a:true` 인데 `lv` 키가 없으면 옛 기록).
    ⚠️ `lv` 는 0 이어도 **항상 저장한다** — 생략하면 새 기록이 옛 기록으로 오해돼 공짜 정복이 된다.
  - 이유: 채집함 50~100칸을 채워도 실제로 쓰는 건 6마리(펫3+결투3)뿐이고 나머지는 합성 재료·
    분해감이었다. 수집 게임인데 수집이 보상되지 않았다. **곤충이 사라져도 도감은 남으므로**
    모으는 행위 자체에 보상을 걸 수 있는 유일한 축이다.
  - 갱신 지점은 **`SaveController._commit` 한 곳**이다. 보유 곤충을 훑는 파생 집계라, 획득 경로
    (실시간 처치·오프라인 정산·짝짓기 수령·서버 세이브 채택)를 하나도 빠뜨리지 않는다.
    ⚠️ 상한 정리(trim) **전에** 훑어야 잘려나갈 곤충도 기록된다.
  - 보상 두 겹: 마일스톤 일시금(발견/정복 N종) + **정복 수 비례 영구 보너스**. 수치는 `dex.json`.
    발견이 아니라 정복에 영구 보너스를 거는 이유 = 발견은 드롭 운이고 정복은 부화기를 돌린 결과다.
  - ⚠️ 영구 보너스도 **§7 기준 밖**이다 — 기준에 넣으면 채울수록 몬스터가 세져 모을 이유가 없어진다.
  - **보스 수집(2026-09-15 사장님 확정)**: 사냥터 보스 44마리(난이도 4 × 11)를 **난이도마다 따로** 모은다.
    기록은 `SaveGame.bossDex`(보스 아트 id), 기록 지점은 `SaveController.advanceZone` 한 곳(최종 보스 포함).
    아래 난이도로 내려가 잡아도 수집은 된다(클리어 보상은 없다). 옛 클리어 기록(`clearedChapters`)도
    `collectedBosses` 가 합쳐 센다. 미수집은 검은 실루엣. 보상은 **모은 수 마일스톤 일시금 — 젤리·화석**
    (`dex.json → bossMilestones`). 골드는 난이도마다 규모가 달라 정액이 무의미하고, 영구 능력치는
    90일 표를 흔들어서 붙이지 않았다. 44마리가 끝이라 젤리 수도꼭지 규칙(§2.6)에 걸리지 않는다.
  - 전 종 정복이 공격 +30%·체력 +40%를 넘지 않게 한다(업그레이드 레벨 두어 개 수준).
    더 키우면 도감이 진행의 지름길이 되어 방치 루프를 밀어낸다.
- **드롭 희소성(2026-08-31 확정)**: 야생 드롭은 **등급 가중치**로 종을 고른다
  (`run_config.json → dropGradeWeights`). 예전엔 종 20개를 균등하게 뽑아 희소성이
  **종 개수 비율**로만 정해졌다 — 전설이 2종이라 드롭의 10%였고 활동 중 13분에 한 마리씩
  나왔다. 등급을 올릴 이유도, 뽑기로 팔 것도 없어진다.
  앱(실시간·채집망)과 서버(방치 정산)가 **같은 함수**(`pickDropSpecies`)를 쓴다.
  희귀 천장(`rarePityKills`)은 이 비율에서 다시 계산해야 한다 — 가중치를 바꾸면 함께 본다.
- **육성 시간(2026-08-31 확정)**: 산란·부화 시간은 등급마다 **3~4배씩** 가팔라진다
  (전설 산란 36시간·부화 16시간). 등급이 올라가도 기다림이 비슷하면 젤리로 당길 이유가 없다.
  ⚠️ 타이머는 **즉시완료 젤리와 한 세트**다. 분 정비례 비용이면 전설 산란이 1,000젤리를 넘겨
  아무도 안 쓴다 — `instantJellyExponent`(0.58)로 긴 대기일수록 분당 단가가 싸지게 한다.
  타이머를 만질 땐 실제 젤리 값을 반드시 함께 확인한다(`pets.json → _timeComment`).
- **채집함 상한(2026-08 확정)**: 보유 곤충 **기본 50마리**, 젤리로 **10칸씩 최대 100칸**까지 확장.
  확장 비용은 **첫 100젤리에서 살수록 오른다**(계단식, `pets.json → expandCostGrowth`) — 정액이면
  다 사고 나서 젤리 쓸 데가 없어진다(§2.6). 부화기·짝짓기 확장도 같은 규칙·같은 첫 값(100)이다.
  가득 차면 새 곤충은 **획득 차단**(드롭 스킵 + 배너 안내). 골드·재료·경험치는 정상 지급.
  - 50인 근거: 곤충 수 버프가 `min(bugsCollected, 50)` 이라 **스탯을 깎지 않는다**(확장은 P2W 아님, 편의).
  - ⚠️ 이 상한은 밸런스이자 **세이브 크기의 방어선**이다. 상한이 없던 시절 곤충 3만 마리 =
    세이브 13.6MB 가 쌓여 업로드마다 DB 타임아웃·Cloud Run 요금 폭증을 일으켰다(2026-07 장애).
    상한을 올릴 땐 세이브 크기·업로드 트래픽 영향을 반드시 함께 검토한다.
  - 강제 지점: 앱(획득 지점 + `SaveController._commit`/`build` 정리) **및** 서버(`GameActions.enforceStorage`).
    클라이언트만 믿지 않는다 — 구버전 앱·조작 업로드도 서버가 자른다.
- **등급 필터 · 자동 방생(2026-08-15 확정)**: 받을 **최소 등급**을 정한다(`SaveGame.bugFilterMinGrade`,
  기본 `common` = 전부 받음). 미달 등급은 채집함에 **들어오지 않고 재료로 환산**된다.
  - 이유: 칸이 50~100개뿐이라 후반엔 일반 곤충이 칸을 채우고, 정작 쓸 개체가 획득 차단에 걸린다.
  - ❌ **방생 보상으로 젤리를 주지 않는다.** 젤리는 프리미엄 재화(§2.6)인데 자동 방생은 방치 중에도
    계속 돌아가서, 켜두기만 해도 젤리가 쌓여 IAP 가 무의미해진다. 일반 재료만 준다
    (`pets.json → releaseMaterialByGrade`). 손으로 하는 분해는 젤리 그대로 — 사람의 손이 병목이라 안전하다.
  - 강제 지점: 앱 획득 지점(실시간 처치·채집망 씬) **및** 서버(`GameActions.settle`).
    채집망 씬은 **필터에 걸린 종을 아예 스폰하지 않는다** — 직접 겨눠 휘두른 걸 방생하면
    "잡았는데 없어졌다"가 된다.

### 2.2 부위 강화
| 부위 | 효과 | 재료 |
|------|------|------|
| 뿔·큰턱 | ATK +4%/Lv | 채집 부산물 |
| 표피 | DEF +4%/Lv | 〃 |
| 날개 | SPD +3%/Lv, 회피 +0.3%p/Lv | 〃 |
| 체격 | HP +5%/Lv | 〃 |

재료 종류: `키틴조각 / 미네랄 / 수액결정 / 곤충젤리`.

### 2.3 전투 (core_battle, 완전 결정론)
> ⚠️ 2026-07 개편: 기존 "치기/집기/던지기" 상성을 **공/방/회 스탠스 + 오행 + 기력**으로 교체(사장님 합의).
- 3마리 팀, **1:1 순차전**, 라운드제(최대 20R), **시드 기반 완전 결정론**.
- **스탠스 RPS**: 매 라운드 **공격 / 방어 / 회복** 중 선택(기질 가중치 확률) → 상성 **공격 > 회복 > 방어 > 공격**.
- **기력(에너지)**: 시작 1, 최대 3. 공격 `+1` / 방어·회복 `−1`. 기력 0이면 **공격만** 가능(무한 힐·방어 스톨 방지).
- **오행 상극(克)**: 내 속성이 상대를 克하면 데미지 **×1.5** (水火·火金·金木·木土·土水).
- **오행 상생(生) 순서 시너지**: 편성 순서에서 앞 슬롯이 뒤 슬롯을 生하면 팀 공격/회복 배율 **연결당 +10%**
  (木火·火土·土金·金水·水木) → **순서가 전략의 핵심**.
- 데미지: `DMG = ATK × (스탠스 조합 배율) × (상생 시너지) × 1.5(상극 시) × 100/(100+DEF)`. 회복 = 최대HP의 일정%.
- **주특기(치/집/던)** = 개체의 **선호 스탠스**(공/방/회), **기질** = 오토 스탠스 성향.
- 20R 종료 시 **HP% 판정**, 동률이면 **SPD 승**.
- API: 오토 `simulate(seed, teamA, teamB) → BattleResult`, **수동 `initBattle()+step(playerAStance:)`**(심리전 화면).
- 스탠스별 조합/배율·기력 규칙은 `core_battle/lib/src/core_battle_base.dart` 참조.
- ⚠️ **결정론 필수**: 모든 무작위는 주입된 `Random(seed)` 로만. 전역 `Random()`/시간 기반 시드 금지.

### 2.4 채집 (방치)
- **사냥터·보스 구조(2026-09-14 확정, `docs/design_zones.md`)**: 난이도마다 **사냥터 10 + 최종 보스 1**.
  사냥터 안의 몬스터는 세기·보상이 **평탄**하고 무한히 나온다. 100마리를 잡으면 **보스 도전**이 열리고,
  깨면 다음 사냥터(몬스터도 보상도 한 계단 위). **쓰러지면 그 사냥터 게이지(100마리)가 비워진다**
  (2026-09-18 사장님 변경 — 그 전엔 실패 벌칙이 없어 게이지를 들고 바로 재도전했다).
  ⚠️ 보스는 "겨우 잡히는 체력"으로 맞춰 둬서 실패가 잦다 — 한 번 질 때마다 100마리를 다시 채운다.
  90일 표(§2.4 난이도별 성장 목표)는 이 벌칙을 **넣지 않고** 뽑은 값이다. 진행이 눈에 띄게 느려지면
  이 규칙부터 되돌리고 표를 다시 본다. 몬스터는 **고정 세기**(적응형 끔) —
  내가 성장해서 넘는다. 구현은 스테이지 축 재사용: 사냥터 k = 스테이지 (k-1)×100+1, 사냥터 안에서는
  스테이지가 오르지 않는다. 진행도 리셋은 `kZoneEpoch`(앱·서버 로드 양쪽 `applyZoneEpoch`).
- **난이도별 성장 목표 · 사냥터 표(2026-09-15 사장님 확정)**: 극한 최종 보스까지 평균 유저(활동 2h + 방치 8h/일)
  **약 90일** — 쉬움 14 · 보통 21 · 어려움 25 · 극한 30일. 난이도마다 최종(11번째) 보스를 잡을 때
  **채움**(강화 레벨·펫·장비·도감을 각각 '현실적 최고치'까지 몇 % 채웠나)이 **50 · 65 · 75 · 85%**.
  - '현실적 최고치' = 강화 maxLevel · 전설 곤충 3마리(포텐셜5·부위 강화 만렙·훈련 80, 특성·이색 제외) ·
    호박 8부위 **상위 10% 롤** · 도감 전 종. 레벨은 뺀다. 정의는 `core_run/tool/balance_targets.json`.
  - ⚠️ **전투력(곱)으로 재지 않는다.** 강화 5종·펫·장비·도감이 서로 곱해져 50% 만 맞춰도 모든 항목이 87% 를
    넘어야 한다. ⚠️ **채움 평균으로 표를 맞추지도 않는다** — 이월되는 장비·펫이 넘치면 강화를 거의 안 사도
    목표를 넘는다. 표는 **강화 채움**(회차마다 초기화되는 유일한 축)을 목표에 맞추고 평균은 결과로 본다.
  - 표는 **난이도별**(`run_config.json → zoneTiers`)이다. 한 표 × 회차 배율(체력 ×3·위협 ×2·보상 ×3 의
    회차 제곱)로는 어려움 303일·극한 미완주였다(장비·펫은 이월, 강화만 초기화라 난이도마다 곡선이 다르다).
    뽑는 법: `balance_sim --fit-tiers --fit-days=14,22,26,34`(2026-09-22 스킬 보스전 규약 반영 후 · 시뮬 결과 14·21·25·29일). 손으로 한 칸만 고치지 않는다.
  - 규칙: 일반 몬스터·위협 = **도착 전력**(8대 · 한 대 12%, 난이도 첫 사냥터는 4대 · 7% — 강화가 막 초기화된
    직후라 가장 약하다). 보스 = 머문 뒤 전력으로 **버틸 수 있는 시간 안에 겨우 잡히는** 체력 — 위협이
    적응형이라 버티는 시간이 거의 일정해서, 전력이 조금만 모자라도 못 잡는 확실한 관문이 된다.
    골드 = 그 난이도 끝에 강화 채움이 목표에 닿는 규모(사냥터마다 ×1.6).
  - 적응형 위협의 회차 배율은 표의 `threatAdaptMult`(1.0·1.15·1.3·1.45). 예전 `2.0^회차` 는 극한 한 대가 96%.
  - **공방 최대 19(화면 20등급) · 13레벨부터 두 계단만 등급업 골드 ×15, 그 뒤 ×1.6**(`forge.json → levelUp.lateFrom·
    lateSpan·endGoldGrowth`). 한 배율(×2.4)이면 극한에서 최상급 장비가 흔해져 장비가 최고치의 176% 로 넘쳤다(→ 92%).
    ×15 를 끝까지 걸면 17 이상이 1경~30경이라 사장님 계정(21등급)이 닿을 곳이 없어졌다 — 끝 구간은 완만하게 두어
    극한 진입 후 약 72일에 20등급(`balance_sim --endgame-days`). 넘은 계정은 최대로 본다(화면·확률 둘 다 clamp).
- **난이도 이동(2026-09-15 사장님 확정, B안)**: 가 본 가장 높은 난이도(`SaveGame.maxTierReached`)까지는 로드맵에서
  **자유롭게 오간다**(성장 초기화 없음). 성장 축 초기화는 **처음 가는 난이도**에 들어설 때만(`tier_progress.dart`).
  아래 난이도에서는 클리어 보상이 없다(앱 `grantChapterClears` · 서버 `_chapterGrantAllowance` 둘 다).
  - 사냥터 전환(`applyZoneEpoch`)은 **난이도를 유지**하고 그 난이도의 사냥터 1 로 보낸다 — 전원 쉬움으로 보내면
    옛 규칙에서 쌓은 강화를 회차 전환으로 한 번 더 잃는다. 어려우면 아래로 내려가면 된다.
  - ⚠️ 동기화 판정(`_localIsAhead`)과 서버 허용치는 **지금 난이도가 아니라 `topTier`** 로 잰다 — 난이도가 합법적으로
    줄어들 수 있어서, 지금 난이도로 재면 내려간 직후 서버 옛 세이브에 덮인다. 서버는 저장본 최고 난이도 +1 까지만 믿는다.
  - **사냥터 클리어 보상 = 그 사냥터 30분치 골드**(`chapterClearHours` 0.5, `chapterClearGold`), **난이도마다 따로**
    받는다(기록 키 `w3@2`). 옛 정액(2만~8억)은 표와 규모가 안 맞았다. 앱 지급·서버 허용치·시뮬이 같은 함수.
    낮은 사냥터 보스를 다시 잡아도 클리어 보상은 없고(기록이 남는다) 젤리도 없다.
  - ⚠️ 펫·도감 축적은 아직 **날짜별 가정 곡선**이다(장비만 공방 규칙으로 계산). 브리딩·훈련으로 실제 닿는지 검증 전.
- **현행**: 횡스크롤 방치 런(`core_run`) — 서식지/보스 자동타격으로 곤충·재화 획득, **오프라인 보상 상한 8시간**.
- **접속 보너스(2026-08 확정)**: 직접 잡은 골드에만 `onlineGoldBonus` 가 붙는다(현재 +60%).
  방치(효율 0.3 × 8h)가 활동 2h보다 커서 **켜두는 것보다 꺼두는 게 이득**이었다.
  방치 보상을 깎으면 "안 켜도 벌린다"를 보고 온 유저가 이탈하므로, 깎지 않고 온라인에 얹는다.
- **레거시**: v1 트랩 채집(`core_gathering`, 필드×트랩 출현 테이블)은 **현재 UI 미사용**(세이브 호환용 잔존).

### 2.5 브리딩(짝짓기) (구현됨)
- 같은 종 **♂+♀ 성충** → 산란 타이머(등급별) → **알** 획득 → 부화기(§2.7)로 유충~성충 육성.
- 사이즈: **부모 평균 ± 변이**(정규분포), **돌연변이 5%**(사이즈 보너스).
- 포텐셜 상속: 높은 부모 기준 **유지 60% / 상승 10% / 하락 30%**.
- 슬롯제(젤리 확장)·젤리 즉시완료. 부모는 잠기지 않음(스냅샷 저장). `IndividualBug.breed()`.

**오행·기질 상속 + 혈통 특성(2026-08-15 확정)** — 짝짓기에 메리트를 만드는 축.
- 문제: 예전엔 오행·기질이 **균등 재추첨**이라 짝짓기 자식이 야생 드롭과 구분되지 않았다.
  오행 상생 순서가 편성 전략의 핵심(§2.3)인데 원하는 속성을 노릴 수단이 없어 돌릴 이유가 없었다.
- **오행·기질은 부모에게서 물려받는다**(`breedingElementInherit` / `breedingTemperamentInherit`).
  **부모가 같은 값이면 자식도 그 값이 확정** — 이게 "계통을 만든다"의 근거다.
- **혈통 특성(`BugTrait`)**: 맹렬(공격) / 강인(체력) / 강건(양쪽 소폭) / 고귀(양쪽 큰 폭).
  **짝짓기 자식만** 가질 수 있고 야생 롤은 항상 없음 — 키운 것과 주운 것을 가르는 표식이다.
  부모에게 있으면 상속(`breedingTraitInherit`, 둘 다 같으면 확정), 둘 다 없으면
  `breedingTraitNew` 로 새로 열린다(1세대 진입로). 계수는 `pets.json → trait*`.
- **특성 효과는 두 곳에 실린다**: 애완펫 기여(방치)와 **PvP 전투 스탯**(2026-08-15).
  전투 쪽 배율은 `traitBattleScale` 로 **따로 조절**한다 — 전투는 부위 강화가 이미 최대 +200%라
  같은 %가 다르게 느껴지고, 문제가 생기면 전투만 0으로 끌 수 있어야 한다.
  - ⚠️ 특성은 **위조를 막을 수단이 없다**(곤충 롤이 기기 권위 §2.1). 다만 이미 위조 가능한
    포텐셜 5(→ 강화 상한 50레벨 = +200%)보다 효과가 작아 **기존 구멍을 조금 넓히는 수준**이다.
    서버 발급으로 전환할 때 함께 봉인한다(그때는 서버가 발급 시 서명을 붙이면 된다).
- 부모 스냅샷(`BreedingSlot.from`)에 오행·기질·특성을 함께 찍는다 — 부모를 잠그지 않으므로
  수령 시점엔 이미 없을 수 있다. 스냅샷이 없는 **개편 전 슬롯은 예전처럼 랜덤**으로 떨어진다.
- 자식 롤 공식은 `BreedingSlot.hatch()` 한 곳 — 앱과 서버가 **같은 함수**를 쓴다.
  ⚠️ `IndividualBug.breed()` 의 rng 소비 순서를 바꾸면 **돌고 있던 슬롯의 결과가 달라진다**.

### 2.6 수익화
- **광고 없는 운영 + IAP** (2026-08-18 확정 — 애드몹 무효 트래픽 정지 후 사장님 결정).
  - 수익은 IAP 만: 젤리 팩 / 스타터 패키지 / 곤충학자 패스 / 스킨. 시즌패스는 다음 축.
  - 예전 "광고 보고 받기" 자리는 전부 **무료 지급 + 하루 상한**으로 전환(문구도 "무료" 계열).
    티켓 무료 충전 5회/일(battle.json), 스카우트 새로고침 10회/일(기기), 대회 참가권 2회/일.
  - `REAL_ADS=on` 으로만 광고 복귀 가능(기본 off). 복귀 시 ARB 문구를 광고용으로 되돌리고
    `docs/ad_policy.md`(무효 트래픽 수칙)를 반드시 따를 것. 새 애드몹 계정 재가입 금지.
  - `remove_ads` 상품은 판매 중단(`hidden`) — 정의는 남긴다(기보유자 매일 젤리 10 지급이 참조).
- ❌ **스탯 직접 판매 금지** (P2W 방지 원칙).
  - 각주(2026-08-31, 사장님 확정): **곤충 알 뽑기(가챠)는 이 원칙의 예외가 아니라 적용례다.**
    젤리 30 = 알 1개. 고급+ 보장 · 이색 1/30(야생의 10배) · 10회 천장(영웅+).
    파는 것은 드롭이 이미 주는 물건의 **시간 절약**이고(포텐셜 분포도 야생과 동일),
    PvP 는 서버 편성 검증·적응형 체력이 완충한다. 수치는 `pets.json → gacha*`.
    ⚠️ 뽑기가 파는 것은 **포텐셜**이다(2026-08-31 재설계). 곤충 자체는 안 귀하다 —
    시간당 1,500처치 × 드롭 3% = **1분에 한 마리**다. 반면 야생 공식
    `1 + floor(r*r*4)` 는 **5성이 수학적으로 불가능**하고 4성도 3.4%뿐이라,
    5성은 짝짓기 상승으로만 닿는다. 뽑기는 3성 이상을 보장하고 5성을 8% 준다.
    ⚠️ 등급 확률은 **야생보다 반드시 좋아야 한다** — 초안이 전설 3%(야생 10%)라
    돈을 내면 확률이 나빠지는 상품이었다.

**젤리 수도꼭지 규칙(2026-08-15 확정)** — 젤리는 프리미엄 재화다. 규칙은 하나:
> **무한히 늘어나는 통로에는 젤리를 붙이지 않는다.**

- 젤리가 나오는 곳이 여섯 군데(일일보상·깜짝선물·미션·리그/시즌·광고버프·분해)로 흩어져 있어,
  하나씩 보면 다 적어 보이는데 합치니 **하루 193개** = ₩5,500 패키지를 **1.6일마다 공짜로** 주고
  있었다. 영구 소비처 전부(390젤리)를 이틀이면 다 샀다.
- 무한한 통로 = **분해**(곤충이 무한히 나옴), **깜짝선물**(접속 시간 비례), **광고 버프**(하루 12회),
  **미션**(보상이 `growth^claims` 지수). 넷 다 막았다 →
  분해는 포텐셜 문턱(4성↑)만 젤리·나머지는 재료, 선물은 최상위 티어에만, 광고 덤은 0,
  미션은 `rewardMax`.
- 결과(유저 유형별 — **하나의 숫자로 말하지 말 것**):
  | 유형 | 젤리/일 | ₩5,500(300젤리) | 영구 소비처 390젤리 |
  |---|---|---|---|
  | 신규(브론즈·광고X·1h) | 14 | 21일 | 28일 |
  | 중반(골드·광고 절반·2h) | 24 | 12일 | 16일 |
  | 고인물(다이아·광고O·4h) | 45 | 7일 | 9일 |
- ⚠️ **리그 승급 보상은 계정당 1회**다(`claimedLeagues` 가 시즌 리셋에서 초기화되지 않음).
  상시 수입으로 세면 과대계상된다 — 반복되는 건 시즌 종료 보상뿐이다.
- 젤리 수치를 만질 땐 `core_run/tool/jelly_sim.dart` 를 반드시 돌린다.
  손으로 더하면 반드시 틀린다 — 수도꼭지가 여섯 개라 하나만 보면 다 작아 보인다.
  회귀 방지 테스트: `app/test/jelly_economy_test.dart`(수치가 아니라 **구조 규칙**을 검사).
**젤리 가격 단위(2026-08-31 확정)** — 젤리를 **쓰는** 자리는 5·10 단위로만 떨어진다
(`roundJellyCost`: 5 미만→5, 50 미만→가장 가까운 5, 50 이상→가장 가까운 10).
- 이유: 소비처 대부분이 시간·수량 비례 공식이라 화면에 `13젤리`·`27젤리`가 떴다.
  프리미엄 재화는 **가격표로 읽혀야** 하는데, 유저는 그 값이 계산 결과인지 정가인지 모른다.
- 확정 가격: 전설 산란 **80** · 부화 **50**(영웅 40·25, 희귀 20·15, 고급 10·5, 일반 5·5).
- ⚠️ **수입(보상)에는 쓰지 않는다.** 보상 젤리를 반올림하면 하루 수입이 바뀌어
  `jelly_sim` 결과가 통째로 어긋난다.
- 회귀 방지: `core_run/test/jelly_cost_test.dart`, `app/test/jelly_economy_test.dart`
  (실데이터의 모든 소비 금액이 5의 배수인지 검사).

- ⚠️ **자동으로 굴러가는 보상에는 젤리를 넣지 말 것.** 자동 방생(§2.1)이 젤리 대신 재료를
  주는 것도 같은 이유다 — 손이 병목인 수동 분해와 달리 자동은 방치 중에도 계속 돈다.

### 2.7 육성·전투 부가 시스템 (현행 구현)
- **부화기**: 알→유충은 부화기 슬롯에서만(등급별 시간·수동 수령, 젤리 확장). 유충→성충은 경과시간 자동.
  즉 **유충은 부화기에 넣지 않는다** — 부화기는 "알 → 유충" 전용 관문이다(`effectiveStage`).
- **수련·돌파**: 성충 레벨업(골드), 상한 도달 시 돌파(재화+타이머, 젤리 즉시완료)로 상한 확장.
  돌파 재료 **종류**는 `game_rules.dart → kBreakthroughMaterials`(키틴·미네랄·수액), **수량**은 `pets.json`.
  앱·서버·비용 표시가 같은 목록을 봐야 한다 — 갈리면 "화면엔 3종인데 서버는 2종만 차감"이 조용히 생긴다.
- **합성 · 자동 합성**: 같은 종 `synthFodder`마리를 소비해 포텐셜 +1. 자동 합성은 이걸 전부 훑는다.
  **재료로 쓰지 않는 것**: 장착·부화 중 곤충, 그리고 **투자한 개체**(수련 레벨 2↑·돌파 티어 1↑·부위 강화 1↑).
  골드를 쏟은 개체가 조용히 사라지면 그게 곧 클레임이다(채집함 상한 정리 `trimBugsTo` 와 같은 원칙).
  실행 전 **예상 결과를 먼저 보여주고 확인받는다**(`autoSynthesize(dryRun: true)`).
- **PvP(곤충 결투)**: 성충3 팀, **스카우트 보드(상대 3택1·난이도 티어)**, 오토/수동 2모드, 트로피.
- **결투 티켓(2026-08 확정)**: 결투 1판에 티켓 1장. 최대 10장, **30분당 1장** 자연충전,
  보상형 광고 1회 **+3장(하루 30회)**, 젤리로 즉시 만땅. 수치는 `battle.json → tickets`.
  - 이유: 결투가 무제한이면 트로피 랭킹이 **전력이 아니라 "돌린 횟수" 순**이 된다.
  - **광고제거·패스 구매자도 하루 30회 상한은 동일**하다(§2.6 P2W 방지). 구매자는 광고를
    스킵하고 즉시 받으므로 *시간만* 절약한다 — 결제로 판수를 사지는 못한다.
  - 강제 지점: 앱(낙관 차감) **및** 서버. 티켓·광고횟수는 **서버 소유 필드**
    (`GameActions._serverOwnedKeys`)라 세이브 편집·구버전 앱으로 늘릴 수 없다.
- **리그/시즌**: 트로피→브론즈~다이아 등급·승급 보상, 시즌 종료 시 소프트리셋+최고등급 보상.
  - **주간 시즌 · KST 월요일 09:00 리셋(2026-08 확정)**. 요일·시각 앵커라 **모든 유저가 같은 순간**에 끝난다
    (예전엔 각 세이브의 시작+N일이라 사람마다 시즌이 달랐다). 기기 시간대가 아니라 고정 오프셋을 쓴다 —
    시간대를 바꿔 시즌을 늘리거나 보상을 두 번 받는 우회를 막는다. 수치는 `battle.json → season`.
- **랭킹 3축(2026-08-15 확정)**: 트로피 / **캐릭터 레벨** / **도달 스테이지**(`RankingKind`).
  ⚠️ **회차 전환이 두 축을 되돌린다**(스테이지 1·레벨 1). 그대로 두면 회차를 넘긴 유저가
  두 랭킹에서 꼴찌가 되어 **넘어갈 이유가 사라진다.** 둘 다 "잃지 않게" 만든다 —
  **레벨·진행도 모두 회차를 1차 정렬 키로** 둔다(`profiles.tier`) — `보통 Lv1` 이
  `쉬움 Lv70` 보다 위다. 화면에도 회차를 앞에 붙여(`쉬움 Lv70`) "왜 저 사람이 위인지"가 보인다.
  ⚠️ 레벨을 **누적(합)**으로도, **역대 최고 기록**으로도 두지 않는다. 둘 다 시도했다가 접었다:
  누적은 화면 레벨(42)과 랭킹 값(250)이 달라 그 자체가 버그로 읽히고, 최고 기록은
  **쉬움에 눌러앉아 레벨만 올리는 것**이 최적이 된다(2026-09-01). 회차를 1차 키로 두면 둘 다 없다.
  트로피 하나만 두면 **결투를 안 하는 유저에겐 랭킹이 없다** — 이 게임의 주된 플레이는 방치 런이다.
  `PvpBackend.leaderboard(kind:)` 가 축을 받고, **정렬은 서버(RPC)가 한다** — 상위 N 을 받아
  클라가 다시 정렬하면 이미 트로피 기준으로 잘린 목록이라 틀린다. 리그 뱃지는 트로피 축에서만 표시.
  - **진행도 = 회차 → 사냥터 → 전투력(2026-09-15 사장님 확정)**. 표기는 `보통 · 사냥터 3` + 둘째 줄
    `전투력 1.2M`(`2-001` 은 폐기). 사냥터 구조에서는 스테이지가 사냥터 시작점으로만 움직여 같은 칸에
    사람이 몰리는데, 예전 정렬은 동률 순서가 임의라 새로고침마다 바뀌었다. 전투력은 **홈 상단 전투력과
    같은 함수**(`displayCombatPower`)다. 앱이 모르면(null) 올리지 않는다 — 0 으로 덮어쓰면 서버 값이 지워진다.
    진행도는 **역대 최고(`bestStage`)** 다(로드맵에서 내려가도 랭킹이 안 떨어진다).
- **왕충 선발대회 뱃지 · 명예의 전당(2026-09-15 사장님 확정)**: 회차가 끝나면 **1위 `champion` ·
  2~10위 `finalist` · 한 판이라도 뛴 나머지 전원 `participant`**. 뱃지 id 는 `종류:회차번호`.
  - 표시 칸은 하나(`profiles.badge`) — **대표 뱃지 = 급 → 최근 회차**(`bestEventBadge`, 앱·서버 공용).
    나중 회차 뱃지로 그냥 덮으면 1회차 챔피언이 2회차 참가로 내려앉는다.
  - 순위표·채팅·명예의 전당에 뜬다. 채팅 뱃지는 **DB 트리거가 넣는 순간 `profiles.badge` 에서 찍는다**
    (앱이 보내게 두면 누구나 챔피언을 단다). `profiles.badge` 는 권위 서버만 쓴다.
  - ⚠️ 뱃지 번호는 **그 유저가 뛴 회차의 번호**다(`EventConfig.roundNoOf`). 이번 설정의 번호를 쓰면
    다음 회차를 연 뒤 접속한 지난 회차 입상자가 다음 회차 뱃지를 받는다. 그래서 끝난 회차는
    `event.json → pastRounds` 로 옮겨 적는다(명예의 전당도 여기서 지난 회차를 찾는다).
  - ⚠️ **회차가 끝나면 점수를 받지 않는다**(끝나기 전에 시작한 판도 기록 안 함). 보상은 각자 접속할 때
    순위를 확인해 주므로, 끝난 뒤 점수가 바뀌면 챔피언이 둘이 될 수 있다.
  - 회차 사이에는 대회 화면이 **대회 대기중(다음 회차 번호·기간·D-day) + 명예의 전당**(1~3위 시상대 ·
    4~10위 · 참가자 전원, `/event/hall`)이다. 2회차 = 2026-10-05 ~ 10-18(KST, 2026-09-25 한 주 미룸), 보상은 1회차와 같다.
- **적응형 몬스터 체력(2026-08 확정)**: 몬스터 체력이 **내 영구 전력(업그레이드+펫)** 에 맞춰 함께 오른다.
  - 이유: 공격은 업그레이드 레벨당 ×1.15, 체력은 스테이지당 ×1.009 라 공격이 항상 이겨 **스테이지 100부터
    한 방**이 됐다(실측). 수치로는 못 고쳐 수식을 바꿨다.
  - **지수 < 1**(현재 0.88) — 강해질수록 타격 수는 줄어든다. 1.0 이면 성장 실감이 사라진다.
  - **기준 1타는 `baselineHitPower`(치명타 포함)** — 생 `attack` 이 아니다(2026-08-15 수정).
    치명타는 타격 수를 그대로 나누므로, 빼고 재면 설정이 "6대"여도 화면은 2~3대다.
    들어가는 것 = 공격력·치명타·보스데미지(펫 포함). 빠지는 것 = **공격속도**(타격 수가 아니라 시간).
  - **월드 관문(worldHpMult)에는 보정을 걸지 않는다** — 걸면 벽이 뭉개져 "뚫는 맛"이 사라진다.
  - **버프·탭 부스트·장비는 기준에서 뺀다** — 넣으면 켜거나 입을 때 몬스터도 세져 효과가 상쇄된다.
  - **`hpAdaptMaxRatio` 는 벽을 뚫는 밸브다**. 무한대로 두면 관문 배율(×8/월드)이 타격 수에
    영구히 곱해져 4월드부터 100대씩 때린다(실측 660일). 올릴 땐 int64 포화도 같이 본다
    (현재 3000 에서 최종 월드보스가 3.3e18 — 한계 9.2e18 까지 여유 2.8배뿐).
  - **위협 기준 맷집은 캐릭터 몫이다(2026-09-15 버그 수정).** 곤충 체력은 팀 체력을 늘리지만 그만큼 캐릭터 몫
    (`playerHpMult`)이 줄어 캐릭터 체력은 그대로인데 한 대는 캐릭터가 받는다. 팀 맷집으로 재던 시절, 곤충
    3마리(체력 ×3.35) 계정은 일반 몬스터 한 대가 캐릭터 체력의 51% 였다(곤충 체력을 빼면 15%).
  - 밸런스를 만질 땐 `core_run/tool/balance_sim.dart` 를 반드시 돌린다. 이 도구는 **맨몸이 아니라
    펫·버프·일일보상까지 포함한 실제 유저**를 재야 한다(맨몸으로 재서 크게 틀린 적이 있다).
- **피격·처치 회복 — "죽을 듯 말 듯"(2026-09-14 사장님 확정, 2026-08-15 규칙 대체)**:
  천천히 닳는 게 아니라 **크게 맞고, 죽여서 되찾는** 리듬이다. 수치는 `run_config.json`.
  - **첫 물기(`enemyFirstBiteDelay`)**: 몬스터는 달라붙고 0.3초 뒤 **반드시 한 대** 문다. 그 뒤
    `enemyAtkInterval`(2.5초)마다. 게이지 이월만으로는 장비를 갖춘 유저가 1초에 잡아 평생 한 대도
    안 맞았다(스테이지 800 실측). 첫 물기가 "마리당 한 대"를 보장한다. 첫 물기 뒤엔 게이지를 0 으로.
  - **한 대가 크다**: 초당 위협 × 간격 = 최대 체력의 약 12%(`threatAdaptTargetPct` 0.048 × 2.5초,
    2026-09-14 실기기 튜닝). 간격이 길수록 같은 DPS 가 한 대로 뭉친다. 1.5초마다 1~2% 는 가랑비라
    긴장이 없었다. ⚠️ 간격만 바꾸면 총 피해가 그대로라 체감이 안 변한다 — 둘을 함께 만진다.
  - **한 대는 캐릭터가 전부 받는다.** 곤충은 `petBiteShare`(0.25)만큼 **따로** 받는다(캐릭터 몫을
    빼앗지 않는다). 팀 체력 비율로 나눠 받던 시절엔 시뮬의 "한 대 12%"가 게임에선 4% 였다.
  - **위협은 적응형, 체력은 사냥터 표(고정)**: 체력 표만 두면 그 사냥터에서 크는 동안 한 대가 22%→1% 로
    묽어진다. 적응형은 한 대를 늘 "지금 맷집의 일정 비율"로 붙든다(표와 적응형 중 큰 쪽).
  - **처치 회복 = 최대 비례 + 잃은 체력 비례**(`killHealPct` 0.02 + `killHealMissingPct` 0.20, 앱·시뮬 공용
    `killHealAmount`). 잃은 만큼 채우니 피격과 회복이 **만나는 높이**가 생겨 체력이 그 근처(30~60%)를
    오르내린다. 벽에서는 한 마리에 여러 대라 그 높이가 0 아래로 내려가 죽는다.
  - **회복(regen)은 최대 체력 비율/초**다(`deriveStats`, 장비 체력 배율도 따라간다). HP/초 절대값이면
    캐릭터 레벨·장비로 체력이 불어나는 만큼 희석돼 후반 초당 0.03% — 회복 195레벨이 죽은 투자였다.
  - **보스 템포 = 일반과 같은 2.5초 · 한 대는 일반의 1.6배(2026-09-15 사장님 확정)**: `bossAtkInterval` 2.5 ·
    `bossThreatMult` 1.6. 예전(5초마다 0.8)은 한 대 크기는 같았지만 느려서 정예보다 약하게 느껴졌다.
    초당 피해가 2배라 버티는 시간이 짧다 — 보스 체력 표는 이 값으로 뽑았다(값을 바꾸면 `--fit-tiers` 다시).
  - 역할 분담: 방어 = 한 대의 크기, 회복 = 사이 메우기, 체력 = 한 대를 견딜 폭. 셋이 각각 다른 일을 한다.
  - 검증은 `balance_sim` 의 **체력 궤적** 표(최저·끝·죽음)로 한다. 수지(합계)만 보면 안 된다 — 천천히
    미끄러지는 것과 오르내리는 것은 같은 수지에서 전혀 다른 게임이다. `--equip-scale=1.5` 로
    장비를 갖춘 유저도 같이 본다. ⚠️ `=3` 은 장비가 공방 규칙으로 계산된 뒤로는 **불가능한 계정**(장비 채움
    228%, 게임은 옵션을 등급 상한으로 자른다)이라 거기서만 나오는 사망은 문제가 아니다(2026-09-15).
- **부상/회복**: 결투 KO 곤충은 등급별 회복 타이머 동안 편성 불가(젤리 즉시회복).
- **버프/미션/일일보상/깜짝선물/펫(장착)/로드맵(난이도 챕터)**: 방치 루프 부가.
- 이 부가 시스템의 수치도 모두 JSON(`buffs/missions/pets/daily/gifts/roadmap/battle.json`).

### 2.8 캐릭터 스킬 (2026-09-15 사장님 확정 — 구현 중, `docs/design_character.md` §4)
- **12종 · 등급 4단계**(일반·희귀·영웅·전설), 액티브·패시브 **공용 5칸**. 처음 2칸, **처음 가 본 난이도**가
  보통·어려움·극한이 될 때마다 +1. 스킬은 **회차가 바뀌어도 유지**한다(장비·펫과 같은 이월 축).
- **성장 = 그 스킬의 조각 + 수련 타이머**(젤리 즉시완료, 돌파와 같은 구조). **해금 조각 100개**.
  ❌ **골드를 쓰지 않는다** — 회차마다 초기화되고 난이도마다 규모가 수만 배 달라 정액 비용이 성립하지 않는다.
- **조각 드롭(2026-09-15 사장님 확정)**: 등급(난이도별 가중치) → 그 등급 스킬 중 랜덤.
  보스 **첫 처치 10개 확정** · 이후 처치는 **10% 확률로 쉬움 3·보통 4·어려움 4·극한 5** · 정예 **10% 확률로 1개**.
  온라인 처치만(방치 정산 없음). ⚠️ 재처치를 확정 지급으로 두면 아래 사냥터 보스(4분에 한 번)를 도는 게
  정예보다 6배 좋아져 앞으로 안 나간다 — 확률로 둔 이유.
- **등급 승급(사장님 확정)**: 같은 등급 조각 10개(어느 스킬 것이든·그 등급 만능 포함) → **한 단계 위 등급 만능 조각 1개**.
  만능 조각은 그 등급 스킬 아무 데나 1:1. 랜덤 드롭이라 무료 전설 1종 해금이 111일 걸리던 문제와 후반 하위 조각 잉여를
  한 번에 푼다. 재료는 유저가 직접 고른다(수련용 조각을 태우지 않게).
- **그 밖의 획득(사장님 확정)**: 하루 무료 뽑기 1회 · **스킬 뽑기 젤리 30 = 한 스킬 조각 10개**(등급 35·30·25·10%,
  10회 천장 전설, 확률 화면 공개) · **보스 소탕권** = 잡아 본 가장 높은 난이도 기준 **조각 확정 3·4·4·5**
  (재처치의 10% 와 달리 확정 — 확률이면 무료 3회가 하루 평균 1개뿐), 하루 무료 3회 · 추가 젤리 10 · 하루 10회까지.
  하루 기록(무료 뽑기·소탕)은 기기 날짜 키라 시계 조작에 약하다 — 알 뽑기·선물과 같은 수준으로 용인.
- **P2W 선(§2.6 적용례 — 알 뽑기와 같은 논리)**: 파는 것은 **시간**이다.
  - 판매하는 조각·수련 단축은 전부 **무료 경로(보스)로도 닿는다.**
  - ⚠️ **결투(PvP)·왕충 선발대회에는 절대 싣지 않는다** — 돈이 트로피와 실물 경품을 정하는 순간이 진짜 P2W 다.
  - ⚠️ **슬롯은 진행도로만 연다**(젤리 판매 금지) — 칸 수는 무료로 못 따라가는 영구 전력이다.
  - ❌ **보유효과(장착 안 해도 스탯)는 없다** — 넣으면 뽑기가 곧 스탯 판매가 되고 도감 영구 보너스와 겹친다.
  - ⚠️ 뽑기 등급 확률은 **어느 난이도의 보스 드롭보다도 좋아야 한다** · **확률 공개 필수**(확률형 아이템 표시 의무).
  - 90일 표(§2.4)는 **무료 유저 기준**으로 맞춘다. 과금 유저가 더 빨리 가는 것이 상품이다.
- **보스 관문 기여(2026-09-22 사장님 확정)**: 스킬 한 칸이 잡을 수 있는 보스 체력을 넓히는 폭 =
  **희귀 약 15% · 영웅 약 25% · 전설 약 40%**(중간 레벨). 방어형(탈피·방벽)도 보스전에 **그대로 적용**한다
  (투자한 의미가 있어야 한다) — 대신 수치로 맞춘다. ⚠️ 보스전은 10초 안팎이라 10초 버프는 싸움 내내 켜진다
  (방벽 8초·질풍 10초·탈피 50% 가 60~130% 였다). 시뮬은 보스전에서 액티브를 **시작 때 준비됨**으로 잰다
  (유저가 도전 버튼을 누르므로) — 평균 가동률로 재면 공격 액티브가 크게 과소평가된다.
  **순간 피해(회심·포충망) = 지금 초당 피해 × 값(초)**(`skillBurstDamage`). `공격력 × 값` 은 공속·치명이 자랄수록
  묽어져 숫자로 못 맞췄다(쉬움 5% → 극한 2%). **끈기(보스 피해) = 곱**(종 패시브와 같은 `applySpeciesPassives`) — 가산이면 강화로 11배까지
  자라는 보스 피해에 묽어져 2% 였다. 여왕의 부름은 곤충 몫(`petShare`)에만 곱한다. 확인: `balance_sim --tiers=4` 의 "스킬 한 칸의 보스 관문 기여" 표,
  조합 비교는 `--loadout=`, 수치 쓸어보기는 `--skill=id.필드=값`.
- **자동발동 100%**(효율 벌칙 없음). 직접 누를 이유는 **타이밍 보너스**(일격: 보스 체력 30% 이하 ×2 · 방벽: 물기 직전
  반사)로 만든다. ⚠️ 재화를 그냥 주는 액티브는 만들지 않는다(쿨타임이 기기 시계라 돌리면 무한 발동).
- **반영 범위**: 온라인 방치 런(`_stats()`) O · **오프라인/서버 정산 X**(펫·장비도 안 들어간다) ·
  **적응형 위협 기준 밖** · 사냥터 표는 스킬 축을 `balance_targets` 에 넣고 `--fit-tiers` 로 다시 뽑는다.
- **밸런스(2026-09-15)**: 시뮬은 스킬을 날짜별 가정 곡선(`balance_targets.json → skillFillByDay`, 무료 조각 수입 기준)과
  로드아웃(`skillLoadout`)으로 넣고, **버프·탭 부스트와 같은 규약**(stats 에 늘 포함)으로 잰다 — 온라인만 넣으면 표를 잴 때
  스킬이 빠져 몬스터가 약하게 뽑혀 77일로 빨라졌다. 스킬을 넣고 `--fit-tiers` 로 zoneTiers 를 다시 뽑았다(90일).
  ⚠️ 곡선은 가정이다(펫·도감과 같은 처지) — 실제 조각 수입으로 닿는지 출시 후 확인.
- 뺀 효과: 오프라인 효율(접속 보너스 역행) · 채집함 칸(칸 확장 상품·세이브 크기) · 골드 %(골드 표·서버 상한) ·
  치명 확률 대폭(치명 예산 `critBudget*`). 되살리려면 그 이유부터 다시 본다.
- 조각은 **새 `MaterialKind` 가 아니라 별도 맵 필드** — 구버전 앱이 모르는 재료 키에서 크래시가 났다.
- **처리 위치 = 알 뽑기와 같다**: 솔로 루프는 기기 권위(2026-07-21 전환)라 뽑기·수련·소탕·보스 조각은 **기기에서**
  처리하고(`core_save/skill_progress.dart` 공용 함수), 서버는 업로드 때 **조각 급증·레벨 상한만 자른다**.
  ⚠️ 위조를 막을 수 없다(곤충 롤과 같은 구멍) — 그래서 결투·대회에 싣지 않는 선이 방어선이다.

---

## 3. 기술 스택 (확정)

| 영역 | 선택 |
|------|------|
| 프레임워크 | Flutter + Dart |
| 상태관리 | Riverpod |
| 로컬 저장 | Hive (`schemaVersion` 마이그레이션 포함) |
| 백엔드 | Supabase (비동기 PvP·리더보드 — **Phase 4**). `PvpBackend` 인터페이스 + `LocalPvpBackend`(자리표시) 완료, Supabase 구현만 남음(키 필요). 가이드: `docs/backend_supabase.md` |
| 세이브 동기화 | 기기 권위 + **60초 주기 전체 업로드**(`ServerSaveUploader`). ⚠️ 주기를 줄이거나 세이브를 키우면 Cloud Run 요금이 **선형으로** 오른다. `/save` 응답은 세이브를 싣지 않는다(`clamped` 일 때만) — 왕복 이그레스 차단. 요청 본문 1MB 초과는 서버가 413. 골드 상식 상한은 강화만 반영한 전력의 **공격 ×100**(처치를 걷는 시간에 붙인다)으로 잰다 — 난이도별 표에서 펫·장비 유저의 정당한 수입이 잘렸다(`tool/clamp_check.dart`). |
| 전투 로직 | `core_battle` 순수 Dart 패키지 (오행·기력·스탠스. Flutter 의존 금지) |
| 방치 수학·설정 | `core_run` 순수 Dart (오프라인 보상·펫·부화기·돌파·버프·전투보상 등 config) |
| 다국어 | ARB (ko/en/ja, **한국어 우선**) |
| 시간 | 서버시간 보정 인터페이스(`clock`) 추상화 (초기 로컬, 추후 교체) |

---

## 4. 폴더 구조 (pub workspace 멀티패키지)

```
BugChamp/
├── CLAUDE.md
├── pubspec.yaml                 # 워크스페이스 루트 (resolution: workspace 멤버 나열)
├── packages/
│   ├── core_models/            # 순수 Dart. 데이터 모델·enum·롤 로직. Flutter/Hive/Riverpod import 금지.
│   │   ├── lib/
│   │   └── test/
│   ├── core_battle/            # 순수 Dart. simulate()/initBattle()/step(). 오행·기력. core_models 에만 의존.
│   │   ├── lib/
│   │   └── test/
│   ├── core_run/               # 순수 Dart. 방치 수학(오프라인 보상) + 설정 모델
│   │   ├── lib/                #   (PetConfig·BattleConfig·Buff/Craft/Enhance/Mission/Daily/Gift/Roadmap).
│   │   └── test/               #   core_models 에만 의존. Flutter 금지.
│   ├── core_gathering/         # 순수 Dart. 레거시 v1 트랩 채집(현재 UI 미사용).
│   ├── core_save/              # 순수 Dart. 세이브 모델·마이그레이션.
│   │                           #   app 과 server 가 **같은 모델**을 쓰기 위해 분리.
│   ├── server/                 # 순수 Dart 권위 서버(shelf). core_* 를 그대로 import.
│   │                           #   재화·전투를 서버가 확정한다. Flutter 금지.
│   └── app/                    # Flutter 앱. UI·Riverpod·Hive·assets·다국어.
│       ├── lib/
│       │   ├── data/           # JSON 로더(game_data), 세이브 리포지토리·마이그레이션
│       │   ├── domain/         # save_controller(세이브 상태·액션), 방치 서비스, clock
│       │   ├── features/       # play / storage / battle / roadmap 등 화면별
│       │   └── l10n/           # ARB
│       ├── assets/
│       │   ├── data/           # ★ 모든 게임 수치 JSON (species·traps·fields·spawns·run_config·
│       │   │                   #    buffs·enhance·craft·missions·pets·daily·gifts·roadmap·battle.json)
│       │   └── images/         # 곤충·서식지·보스·버프·부화기 등 아트
│       └── test/
```

**의존 방향(엄수)**: `{app, server} → core_battle / core_run / core_save → core_models`
(core_gathering 도 core_models 만). **server 는 앱과 같은 core_* 코드로 계산한다** —
로직이 두 벌이 되면 "클라에선 되는데 서버가 거부"가 발생하므로 절대 복제하지 않는다.
core_models 는 어떤 상위 패키지도 모른다. core_run 은 core_battle 을 모른다(형제). 역참조·순환 금지.

---

## 5. 코딩 컨벤션

- **순수 패키지(core_models, core_battle, core_run, core_gathering)** 에서 `flutter`, `hive`, `riverpod`, `dart:ui` import **금지**. 위반 시 아키텍처 위반으로 취급.
- **결정론**: 게임 무작위는 반드시 **주입된 seed 기반 `Random`**. 전역 `Random()`·`DateTime.now()` 기반 롤 금지 (시간은 주입된 clock 인터페이스로).
- **모델**: 불변(immutable). `copyWith`, `fromJson`/`toJson` 제공. (Hive TypeAdapter/영속화는 **app 레이어**에서 처리 — 모델은 저장소를 모른다.)
- **네이밍**: 파일 `snake_case.dart`, 타입 `PascalCase`, 상수 `lowerCamelCase`.
- **텍스트**: UI 문자열 하드코딩 금지 → ARB(`AppLocalizations`). 종 이름 등 게임 데이터의 다국어는 JSON 내 `{ "ko":..., "en":..., "ja":... }` 필드.
- **포맷/린트**: `dart format .` + `flutter analyze` 통과 필수. lints 패키지 기준.
- **테스트**: 사이즈 정규분포 롤·오프라인 보상 계산·전투 결정론은 **반드시 단위테스트** 동반.

---

## 6. ★ 게임 수치 규칙 (최우선 규칙)

> **게임 밸런스 수치(종 스탯, 강화 계수, 출현 확률, 타이머, 드롭율 등)는
> 오직 `packages/app/assets/data/*.json` 에서만 정의·수정한다.**

- Dart 코드에 밸런스 상수를 **매직넘버로 넣지 않는다**. 계수는 JSON → 로더 → 모델 주입.
- 예외: 순수 *구조적* 상수(트랩 슬롯 3개, 최대 20R, 오프라인 상한 8h 같은 **기획 확정 규칙값**)는
  코드 상수로 두되 `game_rules.dart` 한 곳에 모으고 이 파일 §2 와 일치시킨다.
- **이미지 애셋 경로도 데이터로**: 종/필드/트랩 이미지는 `species.json→"image"`, `fields.json→"bg"`, `traps.json→"icon"` 로 참조. 코드에 이미지 경로 하드코딩 금지. 애셋 없으면 UI 가 아이콘/이모지/그라데이션으로 폴백(파일명 = JSON id). 아트 생성 가이드는 `docs/art_prompts.md`.
- 밸런싱 요청 = "JSON 고쳐줘". 밸런싱 때문에 Dart 로직을 고쳐야 한다면 그건 설계 결함 신호.

---

## 7. PowerShell 환경 주의사항 (Windows)

- 셸은 **PowerShell**. 명령 연결 `&&` 는 PowerShell 7+ 에서만 동작 — 안전하게 **한 줄에 하나** 또는 `;` 사용.
- 경로 구분자는 `\` (예: `packages\app`). 스크립트/URL 은 `/` 유지.
- 삭제는 `Remove-Item`, 복사는 `Copy-Item`. `rm -rf` 는 쓰지 말 것.
- 장시간 명령(`flutter run`)은 별도 터미널에서. 인터랙티브 로그인 등은 사용자가 `! <command>` 로 직접 실행.

---

## 8. 자주 쓰는 명령

```powershell
# 의존성 설치 (워크스페이스 루트에서 1회)
dart pub get

# 순수 패키지 테스트
cd packages\core_models ; dart test
cd packages\core_battle ; dart test
cd packages\core_run ; dart test

# 앱: 다국어 생성 / 테스트 / 정적분석 / 포맷
cd packages\app
flutter gen-l10n          # ARB 수정 후 필수(생성 코드 갱신)
flutter test
flutter analyze
dart format .

# 앱 실행 (반드시 packages\app 안에서)
cd packages\app ; flutter run -d <device-id>
```

> 상세 현황·다음 작업은 `docs/PROJECT_STATUS.md`(작업 재개용 스냅샷)를 함께 볼 것.

---

## 9. 로드맵 / 진행 단계

**Phase 1~3 (대부분 완료)**: 모델·전투엔진·방치 루프·저장(Hive/마이그레이션)·UI(플레이/보관함/전투) +
버프·미션·펫·부화기·수련돌파·로드맵·일일보상·깜짝선물·PvP(오행 전투·수동배틀·스카우트·리그·시즌)·아레나 폴리시까지 구현.

**남은 큰 축**
- **Phase 4 (백엔드)**: 인터페이스(`PvpBackend`)·로컬 리더보드·랭킹 화면은 완료. **남은 것 = Supabase 실연동**(프로젝트 생성+키 → 어댑터 연결, 방어팀 등록, 스카우트 실데이터). `docs/backend_supabase.md`.
- **수익화 실연동**: AdMob 실광고·IAP(현재 광고는 플레이스홀더).
- (브리딩 §2.5는 2026-07 구현 완료.)
- 세부 다음 작업·우선순위는 **`docs/PROJECT_STATUS.md` §7**(업그레이드 제안) 참조 — 완료 항목이 체크되어 있음.

**진행 원칙**
- 각 단계 완료 시 **실행·테스트 방법을 보고하고 사용자 승인 후** 다음 단계 진행(단계별 게이트).
- 기능은 **한국어로 먼저 완성**, 타 언어(en/ja)는 나중(영어 폴백).

---

## 10. 개발 자동화 (Claude Code 설정)

이 저장소는 위 규칙 중 **자동으로 강제할 수 있는 것**을 훅·에이전트·스킬로 묶어 두었다.
규칙이 문서에만 있으면 사람도 모델도 잊는다.

| 무엇 | 어디 | 하는 일 |
|---|---|---|
| 아키텍처 가드(훅) | `tool/hook_arch_guard.dart` | 순수 패키지의 Flutter import·시드 없는 `Random()`·`DateTime.now()` 를 **편집 전에** 차단(§5) |
| 편집 후 훅 | `tool/hook_post_edit.dart` | `.dart` 자동 포맷 + 밸런스 JSON 을 고치면 돌려야 할 시뮬을 알려 줌(§6) |
| `balance-guardian` | `.claude/agents/` | 수치 변경의 결과를 시뮬로 재고 §2.6·§7 불변 조건과 대조 |
| 밸런스 목표·도구 | `packages/core_run/tool/` | `balance_targets.json`(90일·채움 목표·현실적 최고치) · `balance_sim --fit-tiers`(난이도별 표) · `zone_check <세이브>`(계정 하나 · 곤충 포함) · `clamp_check`(서버 골드 상한) |
| `save-compat-reviewer` | `.claude/agents/` | 세이브 스키마·서버 동기화 변경의 구버전 호환·세이브 크기 검사 |
| `/balance-tune` | `.claude/skills/` | 밸런스 조정 절차(전 측정 → JSON 수정 → 후 측정 → 회귀 테스트) |
| `/sql-migration` | `.claude/skills/` | Supabase SQL 작성 규칙 + 적용 대장(`docs/sql_migrations.md`) |
| CI | `.github/workflows/ci.yaml` | 포맷·정적분석·전 패키지 테스트 + 의존 방향 검사. iOS/Android 실빌드는 Codemagic 담당 |

훅은 `.claude/settings.json` 에 등록되어 있다. 훅이 막은 편집이 **의도한 예외**라면
사용자 승인을 먼저 받고 가드 스크립트의 예외 목록을 고친다 — 우회하지 않는다.
